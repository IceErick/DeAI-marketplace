# Python integration + research tooling. Pinned loosely for a research prototype.
web3>=6.15,<8            # talk to the blockchain (Anvil / any EVM node)
eth-account>=0.11        # local signing for scripts
fastapi>=0.110          # thin REST API
uvicorn[standard]>=0.29 # ASGI server for FastAPI
streamlit>=1.33         # pure-Python demo UI
requests>=2.31          # IPFS HTTP API calls (Kubo /api/v0)
pandas>=2.2             # collect benchmark results
matplotlib>=3.8         # plot benchmark figures
scikit-learn>=1.4       # produce a tiny real demo model
joblib>=1.3             # serialize the demo model to a file
